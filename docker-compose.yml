services:
  dashboard:
    build: .
    ports:
      - "3002:80"
    env_file:
      - backend/.env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - cache_data:/app/data
    restart: unless-stopped

volumes:
  cache_data:
